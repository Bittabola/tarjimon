#!/usr/bin/env bash

# Activate the Python virtual environment
source venv/bin/activate

# Optional: Add project root to PYTHONPATH
export PYTHONPATH="${PWD}:${PYTHONPATH}"

echo "✅ Activated Python virtual environment for tarjimon"
